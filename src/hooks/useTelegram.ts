// This hook is defined and exported from src/contexts/TelegramContext.tsx
// For modularity, one might create a separate file like this, but it's common
// to co-locate the hook with its context provider.

// To use the hook:
// import { useTelegram } from '@/contexts/TelegramContext';
// or if you prefer a dedicated file:
// export { useTelegram } from '@/contexts/TelegramContext';

// No actual code needed here if co-located. This file serves as a placeholder
// if a separate export file for the hook is desired.
// For this project, useTelegram is directly exported from src/contexts/TelegramContext.tsx.
export {};
